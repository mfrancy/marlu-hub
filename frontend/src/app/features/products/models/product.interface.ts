export interface Product {
    id: string;
    name: string;
    code: string;
    description: string;
    brand: string;
    category: string;
    imageUrl: string;
    createdAt: string;
    updatedAt: string;
}
